services:
  - type: web
    name: enhance-ai-chat-bot-llm-model
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
